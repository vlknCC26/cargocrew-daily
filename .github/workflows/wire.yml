{"name":"hourly-wire","on":{"schedule":[{"cron":"12 * * * *"}],"workflow_dispatch":{}},"permissions":{"contents":"write"},"jobs":{"build":{"runs-on":"ubuntu-latest","steps":[{"uses":"actions/checkout@v4"},{"run":"python3 fetch_feeds.py"},{"run":"git config user.name wire-bot && git config user.email wire-bot@users.noreply.github.com && git add headlines.json && (git diff --cached --quiet || git commit -m wire-update) && git push"}]}}}
